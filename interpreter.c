#include <stdio.h> 
#include <stdlib.h>

/* memory space for pointer */
unsigned char mem[30000] = {0};
/* the pointer itself */
unsigned char* ptr = mem;

void interpret(char* in)
{
    return;
}

int main(void)
{
    return EXIT_SUCCESS;
}
